IMPORT $;
//OUTPUT($.file_accounts.file);
//OUTPUT($.file_persons.file);
OUTPUT($.ChicagoCHS.file);


//Resolução
//IMPORT $, STD, DataPatterns;
//Crimes_raw := $.File_crime_raw.File;
// Tarefa 2
//OUTPUT (Crimes_raw, NAMED('Raw_data'));